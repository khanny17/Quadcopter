apply plugin: 'cpp'

model {
    components {
        AttitudeDeterminator(NativeLibrarySpec)
        main(NativeExecutableSpec) {
            sources {
                cpp.lib library: "AttitudeDeterminator"
            }
        }
    }
}
